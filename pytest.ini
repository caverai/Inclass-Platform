[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Unit tests (no I/O, mock everything)
    integration: Integration tests (httpx.AsyncClient against FastAPI app)
    acceptance: End-to-end acceptance tests (full tutoring flow simulation)
